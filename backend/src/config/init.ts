// This file is kept for backward compatibility but is no longer used for database initialization
// Database initialization is now handled by Prisma migrations and seeding scripts

export const initializeDatabase = async (): Promise<void> => {
  // No-op: Database initialization is handled by Prisma
  console.log('Database initialization handled by Prisma');
};
